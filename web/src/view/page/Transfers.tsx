/*
import * as React from 'react'

export function Transfers() {
	const {requests} = React.useState([] as Request[])

	fetch('requests')
		.then(response => response.json())
		.then(json => console.log(json))
		.catch(err=>{
			console.error(err)
		})
	return (
	<div>
		(requests.map(r=>(
			<div key={r.x}</div>
		))}
	</div>
	}
}
*/
